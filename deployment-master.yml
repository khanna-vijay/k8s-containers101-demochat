---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: demochat_master
  labels:
    service: demochat_master
spec:
  replicas: 1
  template:
    metadata:
      annotations:
        forceRedeployUniqId: "{{CF_REVISION}}"
      labels:
        service: demochat_master
    spec:
      containers:
      - image: verchol/demochat-board:{{CF_BRANCH}}
        name: app
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          protocol: TCP
