apiVersion: v1
kind: Service
metadata:
  name: demochat
  namespace: production
  labels:
    #app: demochat
    #chart: demochat-0.0.1
    #release: prod
    #heritage: Tiller
    sha: "1234"
    branch: "master"
    #repo: "verchol=demochat"
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
      name: demochat
  selector:
    app: demochat
    release: prod
