apiVersion: v1
kind: Service
metadata:
  labels:
    app: demochat-board
  name: demochat-board
spec:
  type: NodePort
  ports:
  - name: "http"
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    service: demochat-board
